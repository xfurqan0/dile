# Dile — project notes and v1 plan

Internal project notes. Kept in English so the repo is readable by everyone.

> **Dile: hold a key, speak, let go. Turkish-first local dictation.**
> Local, open-source, push-to-talk dictation. Windows first, macOS and Linux later from the same codebase. Audio never leaves the machine.

- Repo: `github.com/xfurqan0/dile` · License: MIT
- Name: **Dile** (Turkish "dile getir", to put into words; "dil", language). Maintainer decision 2026-09-07.
- Siblings: [Nazar](https://github.com/xfurqan0/nazar) (agent canvas), [nazar-tray](https://github.com/xfurqan0/nazar-tray) (quota tray). Same stack (Rust + Tauri v2), same repo policy (English everywhere), same rule: **quality over schedule, no deadline; work packages are ordered, one finishes before the next starts.**
- Research phase closed 2026-09-07 04:15: market study (feature matrix of 8 apps, 12 pain-point clusters from issues, Rust/Tauri technical decisions, distribution) + engine study (Turkish ASR landscape, our own test material, filler statistics, M0 protocol). Internal notes hold the full reports (1,659 + 959 lines).
- Order: **third of the three projects to code** (maintainer decision 2026-09-07): **Nazar → nazar-tray → Dile**. nazar-tray is second because it produces the Tauri v2 / CI skeleton this repo copies in WP0; Dile is third because M0 needs a GPU machine and maintainer time.

---

## 1. Why it exists, corrected by the research

The first draft of this file said "no app ships the most accurate engine for Turkish". That is wrong, and the correction matters:

- **Handy already ships Qwen3-ASR (0.6B and 1.7B, GGUF, Apache-2.0) with Turkish in its language list**, next to Whisper and NVIDIA Nemotron 3.5 (Turkish supported). Its engine layer, `transcribe-cpp` (MIT, over `transcribe.cpp`, 16 model families, Vulkan/CUDA/Metal), runs Whisper *and* Qwen3-ASR behind one API. The engine is not the gap.
- **The numbers the first draft quoted were misattributed.** On the FLEURS subset that contains Turkish: Whisper large-v3 **6.85**, Qwen3-ASR-0.6B **10.37**, Qwen3-ASR-1.7B **6.62**. The "16.18" was the 0.6B model, not Whisper. Qwen's Turkish edge exists only at 1.7B and is thin.
- **Fine-tuning is where the accuracy is.** On 320 real Turkish clips, stock large-v3 WER **0.1213** → a 140-hour LoRA (`turkmedstt/whisper-large-v3-turkish-general`) **0.0792**, about 35 % relative. Free training data exists: `issai/Turkish_Speech_Corpus`, 218 h, MIT. On an independent Turkish benchmark (`ysdede/asr_benchmark_store`) `large-v3-turbo` scores **0.2014** vs large-v3 **0.1345**, and the most-downloaded "Turkish" fine-tune (`selimc/…turbo-turkish`) is a regression on 4/4 datasets despite its model-card claim. **Never trust a model card's WER; measure on our own set.**

**So what is the Turkish gap?** Care, not engines. Verified in Handy's source: the Turkish filler list is **empty** while 24 UI locales include Turkish; the dictionary's fuzzy matcher requires ASCII, so every word with `ç ğ ı ö ş ü` is silently dropped. OpenWhispr has no Turkish locale and closed both Turkish PRs unmerged.

**A direct Turkish-first competitor exists: [yusufipk/dikte](https://github.com/yusufipk/dikte)** (found by the maintainer 2026-09-07; audited the same night). Created 2026-07-25; 216 commits in six weeks; 148★, 15 contributors, 5 releases, 24 test files, CI on 3 Pythons × 2 OSes; **GPL-3.0**; Python 3.11 + PyQt6; Linux-first with macOS/Windows ports. It already ships two things this file wrongly claimed nobody had: a **Turkish hallucination-phrase filter** (16+ phrases, duration gate, diacritic-folded matching, `dikte/vad.py`) and a **free-text prompt that biases whisper.cpp and doubles as a glossary** (`config.py`, `api.py`). It also ships a CLI with `--json`, meeting recording with speaker split, file/SRT transcription, history, a live overlay, and **`dikte ask`: speech → a coding agent, with permission modes and session continuity**. Brief mode is not novel any more.

**Where dikte stops, measured against its own source:**
- Turkish cleanup is an **LLM prompt**, default provider **OpenRouter**: text leaves the machine by default; the local option needs llama.cpp and gigabytes of RAM. Its policy is "when in doubt, delete", which contradicts the maintainer's 230-cut edit log (*yani* 1.7–2.1/min and *aslında* 1.0–1.5/min are deliberately kept).
- **No push-to-talk** (toggle only); default hotkey `Ctrl+Space` (the IME conflict Dile rejected); default model `large-v3-turbo-q5_0` (0.2014 WER on Turkish vs 0.1345 for large-v3 in independent data); unsigned on Windows and macOS; Windows GPU path unfinished (open issue); no pre-roll; no locale-aware ı/İ casing; clipboard restore off by default.

**Dile's differentiation, restated after dikte:**
1. **Deterministic Turkish cleanup: no model, no network, no variance, keeps when in doubt.** Three strictness levels derived from real edit data. dikte cannot offer this without abandoning its architecture.
2. **The model is chosen by measurement on our own set, and the dictionary is what makes it accurate** (dikte defaults to the worst Turkish choice). v1 ships stock large-v3 `q5_0` with the dictionary prompt — no fine-tune, because none has earned the row yet. Dile's own LoRA later (WP8).
3. **Push-to-talk done right**: hold and toggle, pre-roll, first-word protection, sane default hotkey, clipboard restored by default, engine isolated, and a GPU path that actually works on Windows because a probe decides it rather than a guess.
4. **Rust/Tauri, signed, small**: one binary, no Python runtime, winget.
5. Brief mode stays on the roadmap as a **feature**, not a differentiator.

**Licence rule (hard):** dikte is GPL-3.0, Dile is MIT. **No code, no snippet, no data file is taken from dikte.** Ideas may be studied and reimplemented independently; every such case is noted in this file with the idea, not the code. Contributing Dile's rule engine to dikte is ruled out: it would hand the only differentiation to a copyleft project.

**Search reality:** "dikte" is the generic Turkish word for dictation, so dikte owns every natural query. Dile is not going to be found by searching for it: discovery is GitHub topics and word of mouth, and the README carries that weight.

## 2. Position and boundaries

| Dile **is** | Dile **is not** |
|---|---|
| Turkish-first local dictation that also works in English | The 20th general dictation app (Handy is that, and it is good) |
| Cleanup and dictionary built for an agglutinative language with diacritics | An English cleanup list with "tr" bolted on |
| Local engine; Vulkan by default behind a first-run probe, CPU as the fallback tier | Cloud STT (Win+H, the dictation built into browsers and chat assistants) |
| v2: brief mode for coding agents | A voice assistant or command runner |
| Audio and text never leave the machine; no telemetry | An account or subscription |
| Push-to-talk dictation into whatever text field has focus | **Meeting recording with speaker split · file/batch transcription · SRT or subtitle export.** Deliberately out of scope (decided 2026-09-07). dikte ships all three; staying narrow is a decision, not a forgotten gap — writing it here is what keeps it from drifting back in |

## 3. Decisions taken in the research phase

| Topic | Decision | Why |
|---|---|---|
| Engine runtime | **`transcribe-cpp`** (MIT) through its Rust crate is **the single decision runtime**: one runtime for Whisper, Qwen3-ASR and Nemotron; Vulkan/CUDA/Metal. **M0 is model-vs-model inside it, never runtime-vs-runtime.** Two consequences, both binding on M0: (a) a Turkish fine-tune that exists **only as CTranslate2** (e.g. `RsGoksel/RsGoksel_ITU_Mainframe`) may be measured from Python **for information only**, and its number cannot select the v1 model until the weights are converted to ggml/gguf and re-measured inside `transcribe-cpp`; (b) **an engine with no Rust path cannot win**, whatever its WER — every candidate row in the M0 table carries a "Rust path" column and an empty one is disqualifying. **`whisper-rs` is not a candidate:** its GitHub mirror is archived/stale, the live Codeberg repo has been quiet for ~6 months, and its two hottest code paths have open bugs (`set_initial_prompt` CString leak #280, VAD broken in `FullParams` #242). `ct2rs` is likewise out (Windows shutdown deadlock open since 2024). | Proven in Handy (31k★). Removes the Python question for Qwen3-ASR (the candle route does not build on Windows with current CUDA; the llama.cpp GGUF route is an unmerged fork). The "Rust path" rule exists because a runtime we cannot ship is a benchmark, not a decision. |
| Model candidates for M0 | Whisper large-v3 (baseline) · `turkmedstt/whisper-large-v3-turkish-general` · `RsGoksel/RsGoksel_ITU_Mainframe` (CV17-tr WER 8.73, MIT; ships as CTranslate2, needs conversion) · Qwen3-ASR-1.7B · Nemotron 3.5 ASR 0.6B (streaming, Turkish, 495 MB). **Excluded:** large-v3-turbo, `selimc/…turbo-turkish` and its three derivatives. | Independent benchmark data above. |
| Engine tiers and defaults | **Vulkan is the default tier and CPU is the fallback** (maintainer decision 2026-09-13, replacing the 09-07 "CPU default, GPU opt-in"). The tier is never assumed: on first start the app runs a **probe transcription inside the isolated engine process**, and only a device that returns a correct result is used. A probe that fails, hangs or takes the process down drops that machine to the **CPU fallback tier with a small model**. The tier stays visible and switchable in settings, and the engine stays in its own process on both tiers. CUDA is not shipped. | The M0 rows decided it, and they point the other way from the 09-07 plan. **Considered and rejected — CPU default:** no whisper model is usable on a six-core CPU (turbo is the fastest and still runs at RTF 2.3, plus ~10 s cold start; q5_0 is ~10 s per dictation), and the small models that *are* faster than real time (Qwen3-ASR-0.6B, RTF 0.27, p95 1.37 s) **cannot take an initial prompt at all** (`accepts_ext(Run,'WHRN') = false`), which removes the dictionary — the product's actual accuracy feature — and leaves WER ≈ 0.491 with 11/31 terms. A CPU-default product would ship the worst version of the thing Dile exists to fix. **Considered and rejected — GPU "Auto":** Handy #1755 (open) has a Vulkan auto-GPU path bug-checking an RTX 5090 (`UNEXPECTED_STORE_EXCEPTION`), and Handy defaults to Auto. A driver-level fault is not something a process boundary can contain, so the answer is a probe rather than a guess, and the toggle stays. Vulkan rather than CUDA on size and hardware: 53 MB against 627 MB, and the official whisper.cpp Windows CUDA builds (11.8 / 12.4) predate Blackwell's sm_120 (needs CUDA ≥ 12.8). |
| v1 model | **Stock Whisper large-v3 `q5_0` (GGUF) with the dictionary fed in as the initial prompt** (maintainer decision 2026-09-13). **No fine-tune ships in v1**, and the README says so. | Measured on the decision layer's 20-sentence set: with the prompt, WER **0.261** and **30/31** technical terms, against 0.379 and 17/31 without it, at p95 2.38 s on Vulkan. **Considered and rejected:** `large-v3-turbo` (0.348, 28/31 with the prompt) saves 1.3 s and is the one row whose Icelandic hallucination survived the prompt; `large-v3` F16 (0.273, the same 30/31) is three times the size, the slowest GPU row, and still loses to its own quantization; every Turkish fine-tune (the most-downloaded one is a regression on 4/4 independent datasets, and the one serious candidate ships as safetensors, which the decision runtime cannot load). These numbers are **final on the 20-sentence set and the robustness set**; the term-heavy slice — the material the decision rule actually names — is **still awaiting the Dilim B reference**, so the pick is settled as a product decision and the term-heavy column is not yet filled. See §4. |
| Hotkey | **Default: `Ctrl+Alt+Space`** (maintainer decision 2026-09-09, replacing the 07 decision of `Right Ctrl`). Detected through `handy-keys` (not `rdev`). **Hold by default:** press → the panel opens and recording starts; release → recording stops. A press shorter than the press-length threshold (~250 ms) is treated as a tap and does nothing except open the panel idle, so an accidental tap never dictates. **Toggle mode is a setting:** tap starts, tap stops. **Optional second key**, still recommended for long dictation: `Right Ctrl` as a modifier-only hold key, because holding a three-key chord for 60 s is tiring. **`Ctrl+Space` is excluded permanently** — the IME chord is swallowed before it reaches even a low-level hook, and dikte already uses it. Re-verified against IME in WP2. **The primary chord is swallowed; the second key never is** (WP2, 2026-09-13): the hook is installed in blocking mode with the chord in its set, because a `Ctrl+Alt+Space` that still reaches the focused window puts a space in the editor being dictated into. The second key stays out of that set — it is a bare right Ctrl, and swallowing it would take `Ctrl+C` away from every application on the machine. | `Ctrl+Alt+Space` is not an IME toggle, not a Windows-reserved chord, and no mainstream editor binds it; it is discoverable in a way `Right Ctrl` alone is not. Before `tauri-plugin-global-shortcut` 2.3.2 the Windows "Released" event was a 50 ms poll that burned a core, which is why `handy-keys` stays. |
| Panel (review UI) | **Dile does not paste blind. Every dictation lands in a panel first.** Maintainer decision 2026-09-09. The panel is a Spotlight-sized floating strip (≈ 680 × 64 px, top-centre of the monitor that holds the focused window, position draggable and remembered), **grey theme**, `focusable: false` (`WS_EX_NOACTIVATE`) so the target window keeps focus; mouse clicks still reach the buttons, and Enter / Esc / Ctrl+C are read from the global hook while the panel is visible. **States:** *recording* — level meter that moves with the microphone RMS (the idiom desktop dictation panels use), elapsed time, the 60 s cap shown, ✗ cancels · *processing* — honest status with elapsed time, never a blank spinner · *result* — the cleaned text, editable in place (clicking into the text is the one action that gives the panel focus; the target `HWND` was captured before the panel opened and is restored before pasting), plus three actions: **✓ transfer** (Enter), **copy** (Ctrl+C; copies without pasting and deliberately does not run clipboard restore, the user asked for the clipboard), **✗ cancel** (Esc). **Default flow after release:** the result is shown and **auto-transferred after 1.5 s unless the user presses ✗ or starts editing**; the countdown is visible. Auto-transfer can be disabled in settings (then ✓ / Enter is required every time). The panel shows the **target application label** ("→ VS Code") so the paste destination is never a surprise, and a **strictness chip** (light / medium / strict) that re-runs cleanup instantly; "show raw" reveals the engine output. **Re-record** button in the result state. | Reviewing before pasting is the one interaction gap that neither Handy nor dikte covers, and in Turkish the ability to fix a mangled proper noun before it lands is worth more than the 1.5 s. Auto-transfer keeps fast dictation fast; the countdown keeps the review possible. Non-activating window: the target keeps focus, so `SetForegroundWindow` restrictions are never hit ([tao `window_state.rs`], market report §4.5). |
| VAD | Evaluate `earshot` (40 KB minGRU, zero deps) vs Silero ONNX in M0. | First-word cut and hallucination on silence are top pain points; VAD quality decides both. |
| Paste | Handy's `paste_tx/windows.rs` pattern (delayed clipboard render, restore previous clipboard), MIT with credit. macOS AX / Linux portals in v2. | Only battle-tested approach in the field; typing key-by-key (WhisperWriter) is slow and lossy. |
| Cleanup corpus | Built from the maintainer's own edit log (see §4), not from assumptions. Repeat-collapsing moves to *strict*. | 70 % of consecutive repeats in real data are legitimate ("güle güle", "tek tek"). |
| UI languages | **EN + TR only in v1** (decided 2026-09-07). The i18n format and policy stay identical to nazar-tray's (`locales/<lang>.json`, flat key/value, no hard-coded strings), so more locales are a translation job, not a refactor. | nazar-tray's six languages are right for a language-neutral quota tray; Dile's audience is Turkish and English, dikte gets by with the same two, and four machine translations would be maintenance debt on a product whose whole point is careful Turkish. |
| Distribution | Tauri bundler (NSIS/MSI) → GitHub Releases + winget; SignPath Foundation application for a free OV certificate, **prepared in WP7 but filed after the first release** (SignPath requires an already-released, actively-maintained project). **Model hosting: Hugging Face only**, pinned by `resolve/<commit-sha>`, sha256 embedded in the app, download `.part` → rename, manual-download instructions documented from day one. **Models are never put on GitHub Releases** and no mirror in v1. Consent dialog before any download; every model's licence and source URL listed in the README. Decided 2026-09-07. | Same path as nazar-tray. GitHub Releases caps files at 2 GiB (`ggml-large-v3.bin` is 2,951 MB) and its AUP forbids "significantly excessive bandwidth"; HF's anonymous rate limit is 3,000 per 5 min per IP and a first run pulls 1–3 files, so hot-linking is legitimate. Handy #1579 (a 47-comment issue) happened purely because manual-download instructions were never written. |
| User research | **No outreach to anyone** (maintainer decision 2026-09-07 05:50). Instead: the maintainer **uses dikte himself** for a period and records structured feedback (what breaks in Turkish, what the LLM cleanup deletes that he would keep, hotkey and paste failures, latency); public issues and discussions of dikte and Handy are read as secondary evidence. | The quantitative case is strong, but the research obtained **zero Turkish users' own words**. First-hand use of the closest competitor is the cheapest substitute for interviews and produces concrete, comparable observations. |
| Hallucination filter | Reimplemented independently in Rust from the MIT phrase dataset: whole-segment match, duration gate, diacritic-folded comparison. | dikte proved the design works in practice (GPL — idea only, no code). |
| M0 additions | `large-v3-turbo` vs `large-v3` becomes a **required pair** in the table (dikte defaults to turbo; we need our own number). A ~130-line relative-RMS VAD (the approach dikte uses) is a free baseline against `earshot` and Silero. | Independent data says turbo is 50 % worse on Turkish; our set must confirm or refute before README claims. |

## 4. Our test material and what the data says

**Audio exists, the reference does not.** 15:16 of the maintainer's own recordings — term-heavy Turkish speech, held locally and never published. The only written material that goes with them was written *before* they were recorded, so it is a script rather than a corrected transcript of what was actually said, and there is no raw/corrected ASR pair anywhere. The reference gets built in M0 with an adjudication method (two engines transcribe, the maintainer only resolves disagreements: ~30–40 min instead of 60–75).

**Where the evidence lives.** Every run quoted in this file was scored in a **benchmark workspace outside this repository**, and that workspace is not published: it holds model weights, the maintainer's own recordings and third-party audio. Run identifiers such as `results/2026-09-09-m0-sentences` appear below so a run can be found again from the maintainer's side; they are not links a reader can open, and nothing in this file depends on opening one.

**Test material, two layers (revised 2026-09-09 — maintainer proposed a YouTube-subtitle set; adopted as a second layer, not a replacement):**

- **Decision layer — the maintainer's own voice and domain.** Shrunk to what only he can supply: the **term-heavy slice** (takes 2, 4, 8, 9; ≈ 5 min; 11.0 technical terms/min), reference built by adjudication (≈ 10–12 min of maintainer time), plus the **20-sentence command set** recorded for this purpose (≈ 10 min): technical terms (`cron`, `SSH`, `CUDA`, `Tauri`, `token`, `cache`), numbers, English-mixed sentences, filler-heavy sentences, punctuation-critical questions. The exact sentences are held with the benchmark material. The clean-monologue and fast-casual slices (takes 1, 3, 7, 10, 11, 12) stay available but are no longer required before the v1 model is chosen. **Only this layer can select the v1 model**, and the technical-term-recall column comes only from here.
- **Robustness layer — independent speakers, zero maintainer time.** 2–3 Turkish technology videos from YouTube with **creator-uploaded subtitles** (auto-generated captions are disqualifying: they are another ASR's output, so scoring against them is circular), single speaker, 30–45 min total. Human subtitles are edited, not verbatim (fillers dropped, sentences tidied), so both sides are normalized before scoring: casing, punctuation, the `eee` filler regex and the particle list of §4 removed → **normalized WER**, used for *ranking* only, never quoted as an absolute accuracy number. Audio and subtitles live in the benchmark workspace, never in this repository, never published. **Agreement rule:** if the model ranking in this layer matches the decision layer, confidence rises; if it differs, that is a finding written into this file, and the decision layer still decides.
  **Assembled 2026-09-09** (94 candidates screened, 13 with a manual `tr` track, 3 accepted; 44:52 total): two TEDx talks with named volunteer transcriber and reviewer (Cem Say on NLP, Merve Ayyüce Kızrak on deep learning; stage lapel mic) and one Kubernetes screencast (desk mic, English terms inside Turkish sentences). Two lessons, binding on WP1: (1) **"listed under Available subtitles" is necessary, not sufficient** — one candidate's manual track spelled its own title word five different ways (`Kübürnetisi`, `kübürnetse`, …), i.e. an ASR output uploaded as a subtitle; every accepted track must be read by a human before use. (2) The edited subtitles contain **zero `eee`** across 5,342 words and thinned particles, so **the filler rule gets no exercise on this layer**; only the decision layer validates cleanup. The first cue of both TEDx files is an unspoken translator credit and is dropped at scoring time.

**Filler statistics, from a real edit log** (230 timestamped cuts over 22:02 of the maintainer's own recordings, held locally and never published):
- Filler events: **1.13 / min**, average **0.27 s**, and **the only form is "eee"**. No "ııı", "ımm", "hmm" at all. The first-draft regex `\b(e+|ı+|ımm+|hmm+)\b` was too broad.
- Consecutive-word repeats: 0.5 / min, **~70 % legitimate** (reduplication idioms, onomatopoeia). Collapsing them is a *strict*-mode rule, not medium.
- Particles the maintainer **keeps** (per minute): *yani* 1.7–2.1 · *aslında* 1.0–1.5 · *evet* 0.3–1.3 · *ya* 0.15–1.0 · *şey* 0.7–1.4.

**Which M0 numbers are settled and which are not** (status 2026-09-13). This table exists so that no figure in this file gets quoted with more authority than it has:

| Measurement | The reference it scores against | Status |
|---|---|---|
| 20-sentence command set: WER, CER, technical-term recall, p50/p95 latency, with and without the dictionary prompt | Written by the maintainer at recording time, so it exists and is exact | **Final.** Every number quoted in §3 and in the README — 0.379 → 0.261, 17 → 30/31 terms, p95 2.38 s — comes from here |
| Robustness layer: normalized WER over the three creator-subtitled videos | Creator-uploaded subtitles, which are edited rather than verbatim | **Final for ranking only.** The absolute values are not comparable with the decision layer and are never quoted as accuracy |
| Term-heavy slice (takes 2, 4, 8, 9): WER, technical-term recall | **Dilim B — adjudicated 2026-09-13.** All 36 disagreement regions resolved: 33 by choosing between engine hypotheses, 3 by hand. 673 normalized reference words, 48 scored term occurrences | **Final on variant (a), the reference exactly as adjudicated** (benchmark workspace, `results/2026-09-13-m0-term-heavy`): `fw-large-v3` WER 0.0267 / 45 terms · `fw-beam5-prompt` 0.0327 / 44 · `tc-large-v3` 0.0342 / 41 · `fw-greedy` 0.0357 / 43 · `tc-q5_0-prompt` 0.0401 / 42 · `tc-turbo` 0.0461 / 43. **Variant (b)** — the same reference with the product-name hints applied — is **pending maintainer confirmation of 8 term hints** and is never quoted as settled. The absolute level is low because the reference was assembled from three of these rows' own hypotheses; only the ordering, the term recall and the all-row misses travel outside this set |
| Filler statistics: 1.13 events/min, 0.27 s average, "eee" the only form | The maintainer's own edit log | **Final**, and already spent: it is what set the §5 filler regex |

The v1 model decision in §3 was taken as a **product decision on the evidence that existed**, before the term-heavy slice the decision rule names had a reference. It has one now, and **the slice does not reverse the decision.** Over the three `transcribe-cpp` rows Dilim B orders `tc-large-v3` < `tc-q5_0-prompt` < `tc-turbo`, against `tc-q5_0` < `tc-large-v3` < `tc-turbo` in both earlier layers: one adjacent swap, ρ = 0.5, worth four word errors out of 673 — and the q5_0 row is the only prompted one of the three while the other two helped build the reference. Paired by exact configuration, the four un-prompted rows reproduce the 20-sentence order exactly (ρ = 1.000). `tc-turbo` is last in every layer now measured. On term recall against the corrected reference the decision layer orders `tc-q5_0-prompt` 37 > `tc-turbo` 36 > `tc-large-v3` 34, so the metric this product exists for points the same way the decision did. The "if Dilim B reverses the ranking it is a finding" clause of this paragraph is therefore discharged rather than invoked; the full run is in §8, 2026-09-13.

## 5. Turkish cleanup rules (v1, revised)

- **Light:** remove "eee" and its lengthened forms — **the default filler regex is exactly `\b[Ee]{2,}\b` and nothing else** (decided 2026-09-07), with duration/position guards from the VAD; any other filler pattern is opt-in in settings, never on by default. Remove the 24 known hallucination phrases when they appear as a whole segment; fix leftover whitespace/comma at sentence start.
- **Medium (default):** + sentence-initial capitalization; + punctuation normalization (comma pile-ups, "?" after question particles *mı/mi/mu/mü* and question words); + dictionary spelling of proper nouns and English terms (non-ASCII safe).
- **Strict:** + collapse consecutive repeats except a reduplication whitelist — **repeat collapsing is a strict-only rule and never runs in medium** (decided 2026-09-07); + drop standalone "şey" before a pause ("bir şey" stays); + drop *ya / yani / hani* at clause edges.
- Diacritics are never stripped; the engine's Turkish output is normalized to proper `ı/İ` casing rules (Turkish `i` ↔ `İ`, `ı` ↔ `I`).

## 6. Work packages (ordered; no dates)

| # | Package | Done when |
|---|---|---|
| WP0 | Repo skeleton: Tauri v2, Rust workspace (`dile-core`, `dile-engine`, `dile-app`), CI (Windows build + tests), locale scaffolding EN/TR | **Code done 2026-09-09**, `39d7068`…`b2dd8bf`; the whole gate passes locally on an empty tray app. Actions was unavailable while the repository was private, so both jobs were rejected before a runner started and the package stayed open on its acceptance criterion — which was *CI* green, not code. **Closed 2026-09-13: CI green on the public repository.** The first run on `main` finished green in 12 m 17 s with no code change, exactly as predicted. `scripts/ci-local.ps1` stays on as the pre-push gate |
| WP1 | **M0 engine experiment.** Protocol, in two layers that are never mixed: **(1) decision layer** — every candidate runs through **`transcribe-cpp`** (the single decision runtime, §3) on CPU and Vulkan; only rows produced here can choose the v1 model. **(2) information layer** — CT2-only Turkish fine-tunes are measured from **Python (faster-whisper / CTranslate2) for information only** and are flagged as such in the table; a strong number there is a reason to *convert* the weights to ggml/gguf and re-measure in layer 1, never a reason to ship. **An engine with no Rust path cannot win:** the table carries a "Rust path" column and an empty cell disqualifies the row regardless of WER. `whisper-rs` and `ct2rs` are not candidates (§3). Also required: build the reference for the term-heavy slice by adjudication, record the 20-sentence set, assemble the YouTube robustness set (§4) with normalized WER, the `large-v3-turbo` vs `large-v3` pair, an RMS-VAD baseline against `earshot`/Silero, and the prebuilt-JIT vs built-from-source (native sm_120) comparison. Columns: WER / CER / technical-term recall / p50–p95 latency / VRAM / Rust path / layer / **normalized WER on the robustness set**. **Decision written into this file**, including whether the two test layers agreed on the ranking. | Table filled with the layer and Rust-path columns populated for every row; one model chosen for v1 by the decision rule in the engine report (lowest WER on the term-heavy slice unless latency > 2× best), and that model is one that ran in layer 1; the robustness-set ranking is recorded next to it |
| WP2 | **Capture**: hotkey (`Ctrl+Alt+Space` by default, hold with press-length threshold + toggle setting + optional second key `Right Ctrl` modifier-only), microphone via `cpal`, live RMS level stream for the panel meter, VAD (winner of M0), **recording cap as a setting — default 60 s, maximum 300 s**, first-word protection (pre-roll buffer) | Hold, speak, release → audio buffer with no clipped first syllable; IME conflict check done on the decided default; the cap is adjustable and stops cleanly at the limit; level events arrive at ≥ 20 Hz. **Implemented 2026-09-13**, `e94d244`…`c920cb4`: the two crates (`dile-hotkey`, `dile-capture`) and the wiring in `dile-app` — the chord is swallowed, the tray carries the state, and a recording with no speech in it is dropped rather than sent on. **Hardware acceptance is pending the maintainer's hand test**: every criterion in this cell is about a person holding a key — the unclipped first syllable, the IME check on a Turkish layout, the cap stopping cleanly — and the local gate ran without a microphone. The recipe is in `docs/BUILDING.md`, "Trying the capture path". The **VAD is the relative-RMS baseline, not the winner of M0**, because that row has not been measured; it sits behind a trait, so the winner is a constructor argument |
| WP3 | **Engine process**: `transcribe-cpp` in an isolated process; the **first-run Vulkan probe** of §3 and the drop to the CPU fallback tier when it fails; crash isolation; model downloader from Hugging Face with consent + checksum + progress | Killing the engine process leaves the app running; a machine whose probe fails ends up on the CPU tier with a small model and says so; a model download resumes after interruption |
| WP4 | **Turkish layer**: cleanup rules (§5) with the three strictness levels, hallucination filter, non-ASCII dictionary that also feeds the engine's initial prompt / hotwords, Turkish casing | Rule tests from the edit-log corpus; dictionary round-trips `ç ğ ı ö ş ü`; "yani" survives medium. **Implemented 2026-09-13**, `58a57ac`…`3a638b8`: a pipeline of eight rules in `dile-core`, each quoting the §5 sentence it implements, with `Strictness` selecting which of them run and a `CleanReport` recording every edit for WP5's "show raw". All three criteria are met by tests: 156 before→after corpus rows (47 light, 48 medium, 47 strict, 14 dictionary) across 9 integration tests, plus 22 unit tests in the crate. The **hallucination list is a seed of 27 phrases** — the 17 rows of the MIT `whisper-hallucinations` Turkish block that cannot be ordinary speech, plus 10 extended from our own runs; the seven declined rows are kept in the source with the reason beside each. **The default dictionary is empty** and stays empty. Nothing about the filler rule was validated on real audio, because no supported engine writes a filler to validate it against (§8, 2026-09-09) |
| WP5 | **Panel + paste + settings**: the review panel of §3 (non-activating grey strip; recording / processing / result states; level meter, elapsed + cap, target-app label, strictness chip, show-raw, editable text, ✓ / copy / ✗ / re-record; Enter / Esc / Ctrl+C from the global hook; 1.5 s auto-transfer with visible countdown, switchable off; position remembered per monitor); clipboard paste with previous-content restore and per-target chord (terminal family → `Ctrl+Shift+V`); paste receipt via delayed render, never a fixed delay; settings window (hotkeys, hold/toggle, auto-transfer, model, GPU toggle, strictness, dictionary, language), autostart | Works in VS Code, Windows Terminal, browser, Electron apps; the target window never loses focus while the panel is up; editing in the panel then ✓ pastes the edited text; clipboard restored byte-for-byte after ✓, untouched after copy; cancel leaves the clipboard exactly as it was |
| WP6 | **i18n**: remaining UI locales, language switch without restart | No hard-coded UI strings |
| WP7 | **Distribution**: NSIS/MSI, winget manifest, SignPath **preparation** (`docs/CODE_SIGNING.md`, CI build from source, MFA, roles — the application itself is filed after the release), README (EN) with GIF, CHANGELOG, first release checklist. Plus a **minimal CLI**: `dile transcribe <file>` with `--json`, and nothing more — no scope race with dikte's CLI | Fresh Windows VM: install → first Turkish dictation in 5 minutes; `dile transcribe --json` prints machine-readable output |
| WP8 | **Dile-tr model** (**after the first public release**, decided 2026-09-07): LoRA fine-tune of large-v3 on `issai/Turkish_Speech_Corpus` (218 h, MIT) plus the maintainer's own data; publish on Hugging Face; re-run M0 table | Beats the v1 model on our set; the training recipe is documented and reproducible |
| WP9 | **Brief mode** (v2): speech → "what / files / acceptance" template; local Ollama or API, user's choice. Not a differentiator (dikte ships `dikte ask`); Dile's angle is the deterministic template with no model required for the structure. | — |

The **dikte usage log** (§3 — the maintainer uses dikte himself and records structured feedback; no outreach to anyone) happens between WP1 and WP2. Going public happens after WP7.

## 7. Decisions closed 2026-09-07, and what is still open

All of the following were open questions in the first draft of this file. They are settled; the reasoning lives in §1–§5 and in the internal reports.

| Question | Decided | Where it binds |
|---|---|---|
| UI locale count | **EN + TR only in v1.** Format and policy stay identical to nazar-tray's, so adding locales later is translation work, not a refactor | §3 UI languages · WP6 |
| WP8 (Dile-tr LoRA) before or after the first release | **After.** It is a GPU-expensive research task; ship with a measured off-the-shelf model and make the LoRA its own story | §6 WP8 |
| Default hotkey | **`Ctrl+Alt+Space`** (revised 2026-09-09; was `Right Ctrl`), via `handy-keys`; hold by default with a press-length threshold, toggle as a setting, `Right Ctrl` modifier-only as the optional second key. **`Ctrl+Space` excluded permanently** (IME swallows the chord; dikte already uses it) | §3 Hotkey · WP2 |
| Paste blind or review first | **Review panel first, auto-transfer after 1.5 s unless cancelled or edited; editable text; copy / ✓ / ✗** (2026-09-09). Auto-transfer can be switched off | §3 Panel · WP5 |
| Model hosting | **Hugging Face only**, pinned by `resolve/<commit-sha>` with sha256 embedded in the app; **no models on GitHub Releases** (2 GiB/file cap, AUP bandwidth clause); **no mirror in v1** | §3 Distribution · WP3 |
| Does Dile get a CLI | **Yes, minimal and late:** `dile transcribe <file>` + `--json` at WP7. Sibling projects all offer machine-readable output; the scope race with dikte's CLI is declined | §6 WP7 |
| Recording cap | **A setting: default 60 s, maximum 300 s.** dikte runs 300 s without complaints; 60 s is short for dictating a long thought | §6 WP2 |
| Meeting recording / file transcription / SRT | **Out of scope**, and written into the §2 table so it stays out | §2 |
| Consecutive-repeat collapsing | **Strict level only, never medium.** ~70 % of repeats in the real edit log are legitimate reduplication | §5 |
| Filler regex | **Exactly `\b[Ee]{2,}\b` by default**, nothing else; other patterns are opt-in. "eee" is the only filler form observed in 230 real cuts, and `ı+` would hit real Turkish words | §5 |
| Default engine tier | **Vulkan by default behind a first-run probe in the isolated engine process, CPU as the fallback tier with a small model** (2026-09-13; replaces "CPU default, GPU opt-in"). A CPU-default product cannot use the dictionary and dictates at WER ≈ 0.49 | §2 · §3 · §6 WP3 |
| v1 model | **Stock large-v3 `q5_0` GGUF with the dictionary as the initial prompt; no fine-tune in v1** (2026-09-13). WER 0.261, 30/31 terms, p95 2.38 s on the 20-sentence set | §3 · §6 WP1 · README |

**Still open:** nothing blocking WP0, which **closed on 2026-09-13** when the first run on the public repository went green. The model and the default tier are decided (the two rows above), and the **Dilim B reference now exists and is scored** (§4, §8) — it confirmed the model rather than reversing it. What is left is measurement rather than judgement: the **VAD winner** and the latency budget. WP0 already settled the build question: `cargo build --features vulkan` needs the Vulkan SDK on the build machine and nothing else. WP2 did **not** settle the VAD: `earshot` against Silero is still an unmeasured M0 row, so what ships today is the relative-RMS baseline the protocol asked for as a floor. It sits behind the `Vad` trait, and the winner arrives as one constructor argument rather than as a rewrite — which is the point of having built the seam before the measurement.

## 8. Log
- 2026-09-07 01:05 — Folder created, git init, first spec. Maintainer: own app, v1, local model, hold-to-talk default + toggle setting, cross-platform later.
- 2026-09-07 01:20 — Name Sesle → Dile. Repo `xfurqan0/dile` private.
- 2026-09-07 01:40 — Notes translated to English; repo language policy: English everywhere.
- 2026-09-07 05:35 — Maintainer found **yusufipk/dikte** (Turkish-first, GPL-3.0, Python, 148★, active). Audit: it ships a hallucination filter, prompt biasing and `dikte ask` (brief mode), so three "nobody has this" claims were removed. Differentiation reframed around deterministic offline cleanup that keeps when in doubt, a measured Turkish-tuned model, proper push-to-talk, and a signed single binary. Hard licence rule: nothing from dikte's code. M0 gains the turbo-vs-large-v3 pair and an RMS-VAD baseline.
- 2026-09-07 05:30 — **Order fixed: Nazar → nazar-tray → Dile; Dile is third.** nazar-tray is second and produces the Tauri/CI skeleton WP0 copies; Dile is third because M0 needs a GPU machine and maintainer time. **M0 protocol made explicit** (§3, WP1): `transcribe-cpp` is the single decision runtime; CT2-only Turkish fine-tunes are measured from Python for information only and cannot select the v1 model until converted and re-measured; an engine with no Rust path cannot win; `whisper-rs` is not a candidate (archived GitHub mirror, ~6 months quiet on Codeberg, open CString and VAD bugs) and neither is `ct2rs` (Windows shutdown deadlock). **Nine open decisions closed** (§7): EN+TR only · WP8 after the first release · Right Ctrl by modifier-only detection with `Ctrl+Alt+Space` fallback and `Ctrl+Space` permanently excluded · Hugging Face pinned by commit sha, no models on GitHub Releases · minimal CLI at WP7 · recording cap 60 s default / 300 s max · meeting, file and SRT work out of scope and written into the §2 table · repeat collapsing strict-only · filler regex exactly `\b[Ee]{2,}\b`. The 05:35 note below planned user research through other people; that is **reversed** — the maintainer's own dikte usage log is the whole of it.
- 2026-09-07 04:15 — Research phase closed. Corrections: engine is not the gap (Handy ships Qwen3-ASR with Turkish); numbers fixed; `large-v3-turbo` and `selimc` fine-tune excluded; `transcribe-cpp` as the single runtime; CPU default / GPU opt-in / isolated engine process; cleanup corpus rebuilt from the maintainer's real edit log ("eee" is the only filler; repeats mostly legitimate); no hand-corrected reference exists yet, M0 builds it. Work packages WP0–WP9. Waiting for the maintainer's go.
- 2026-09-13 — **Term-heavy slice adjudicated and scored; M0 closes, and WP0 with it.** Dilim B finally has a reference: all 36 disagreement regions resolved — 33 by choosing between engine hypotheses, 3 by hand. The slice is 293.5 s over four takes, **673 normalized reference words and 48 scored term occurrences** (proper nouns and English technical terms; the plain Turkish lemma the term list also carries is excluded). Scored in the benchmark workspace, `results/2026-09-13-m0-term-heavy`, with the normalizer and the WER tool of the two earlier layers unchanged, so the three sets are comparable. Six rows, against **variant (a), the reference exactly as adjudicated**:

  | row | layer | prompt | RTF | WER | sub / ins / del | terms / 48 |
  |---|---|---|---|---|---|---|
  | `fw-large-v3` (beam 5) | information | no | 0.088 | **0.0267** | 11 / 0 / 7 | **45** |
  | `fw-large-v3-beam5-prompt` | information | yes | 0.091 | 0.0327 | 19 / 0 / 3 | 44 |
  | `tc-large-v3` | **decision** | no | 0.725 | **0.0342** | 16 / 2 / 5 | 41 |
  | `fw-large-v3-greedy` | information | no | 0.078 | 0.0357 | 18 / 0 / 6 | 43 |
  | `tc-q5_0-prompt` | **decision** | yes | 0.296 | 0.0401 | 20 / 2 / 5 | 42 |
  | `tc-turbo` | **decision** | no | **0.032** | 0.0461 | 21 / 3 / 7 | 43 |

  **A second reference variant exists and it is not a fact.** Adjudication is a vote between engine hypotheses, so it is blind wherever all three engines made the same mistake — the tool can only offer what an engine wrote. The exported term list marks those slots with a hint, and **variant (b) applies only the product-name hints: eight occurrences across five names**. It is written into the results folder, never into the reference, and it is **pending the maintainer's confirmation of those 8 term hints** — it is his open decision, not a measurement. Under (b) every row is 0.003–0.009 WER worse and term recall drops to 34–39 of 48, and the decision layer **reorders**: `tc-q5_0-prompt` 37 > `tc-turbo` 36 > `tc-large-v3` 34, while WER still ranks `tc-large-v3` first. Two things argue the hints are right: the adjudicated reference currently spells one product name two different ways inside the same take, and two of the three regions resolved by hand are exactly this kind of correction.

  **Ranking.** Over the three `transcribe-cpp` rows Dilim B orders `tc-large-v3` < `tc-q5_0-prompt` < `tc-turbo` on **both** variants, where the 20-sentence set (prompted and un-prompted) and the robustness layer all order `tc-q5_0` < `tc-large-v3` < `tc-turbo`: **one adjacent swap, ρ = 0.5**, worth four word errors out of 673 on (a) and two on (b). Two confounds sit on that swap and neither is corrected, only recorded: the q5_0 row here is the only prompted one of the three, and `tc-large-v3` and `tc-turbo` were two of the three engines the adjudication chose between, so the reference is partly their own text while the q5_0 row never had a vote. Paired by exact configuration, the four un-prompted rows reproduce the 20-sentence order **exactly, ρ = 1.000**; across all six rows ρ = 0.543, and against the robustness layer ρ = −0.105 — the independent-speaker disagreement already recorded on 2026-09-09. `tc-turbo` is last in every layer now measured.

  **The prompt is worth one term on this material, and that is the run's useful negative result.** The only clean A/B — same weights, same beam, the prompt the sole variable — moves WER by **+0.0060** on (a) and **+0.0030** on (b), and term recall by **−1 on (a), +1 on (b)**; the ±1 is the same token in both directions, because the prompted row writes the dictionary spelling of a name that (a) scores as an error and (b) as a hit. Exactly one name is recovered by the two prompted rows and by nothing else, and that is the entire measured gain. On the 20-sentence set the same prompt was worth 12–14 terms. The difference is the material: an initial prompt reaches the first decoding window, and five minutes of continuous speech drifts away from it. **WP4 must not assume the 20-sentence gain transfers to long dictation.** Two of the names the slice loses are *in* the dictionary and neither prompted row recovered either of them (0 of 2 and 0 of 4 occurrences); one measured side effect is that the prompted row spelled out three numerals the un-prompted row wrote as digits, so the prompt's own style leaks into the output.

  **M0 verdict.** The term-heavy slice confirms the v1 choice without flattering it: `tc-q5_0` with the dictionary prompt is the WER winner on no single layer, but it is first or second on all four, it is the only `transcribe-cpp` row that is never last, it carries the best decision-layer term recall under the corrected reference (37 of 48 against 36 and 34), and it does that at RTF 0.296 against 0.725 for the F16 large-v3 whose accuracy it matches — the row that beats it on WER here beats it by two to four word errors out of 673 and costs 2.5× the time, which is not a decision-grade gap. `tc-turbo` stays the documented no-GPU fallback and nothing more: last on WER in all four layers, both occurrences of one product name dropped, and the only insertions in the run. One failure mode is new and it is a decoder one, not a vocabulary one — on the single slot where a product name sits next to a platform name, all three `transcribe-cpp` rows collapse into a subscription-shaped Turkish phrase while both faster-whisper rows keep the name legible, which is greedy decoding losing a long context. The residual term error is not a model-selection problem at all: **four product names, nine of the 48 occurrences, are missed by all six rows** — prompted and un-prompted, greedy and beam, quantized and fp16 — because each is heard as a plausible word in the slot, the same mechanism the 20-sentence run recorded and the same one the §5 dictionary rule exists to fix. So M0 closes with the model decided, the decision rule actually run rather than anticipated, and the remaining accuracy work handed to WP4's dictionary rather than to another engine row.

  **WP0 closes on the same day, and not because of code.** Its acceptance criterion was CI green, which the private repository could never produce; the first run on the public repository finished green in 12 m 17 s with no change to the tree. `scripts/ci-local.ps1` stays as the pre-push gate.
- 2026-09-13 — **WP4 is implemented: the Turkish layer is real.** Three commits, `58a57ac`…`3a638b8`. `dile-core`'s modules stop returning their input: `clean(raw, level)` is a pipeline of eight rules, the dictionary works in both directions, and the hallucination filter is in. Still no dependencies — not even a regular-expression crate, although `regex` is already in the lock file through Tauri: the rules of §5 are a handful of scans over a string, and the one crate that has to build everywhere stays free of anything that might not.
  - **The shape is a pipeline, and the order is a decision.** Each rule is a pure `fn(&str, &Config) -> String` whose doc comment opens with the sentence of §5 it implements, and `Strictness` selects which ones run. Nothing in a rule knows about reporting: `clean_with` compares each rule's input with its output and records the difference as a `CleanReport`, so WP5's "show raw" and the maintainer's edit log get a step-by-step replay without a single rule carrying bookkeeping, and a rule that changes nothing costs one string comparison. The order — hallucination, filler, repeats, particles, punctuation, dictionary, whitespace, casing — is written out in the module documentation with its reasons: deletions come before punctuation because dropping a *yani* at a clause end is what leaves a comma in front of a full stop for the hygiene rule to clear, and **casing runs last** because the first word of a sentence is only known once nothing else is going to remove it.
  - **The whole-segment rule, and why it is not caution for its own sake.** A canned phrase is deleted only when it is the entire text or a whole sentence standing alone — never inside a sentence. The 09-09 robustness run is the reason: `beğenmeyi unutmayın` was **genuinely spoken** in the middle of a screencast sentence, and a filter matching on the phrase alone would have eaten it. That finding asked for a cross-check against duration or segment shape; in a crate with no audio in it the sentence is the segment, and the sentence is the cross-check. Comparison folds case and diacritics and the folded form is never written out, so `Altyazı M.K.` and `altyazi m k` are one phrase while every diacritic in a surviving sentence is kept.
  - **The hallucination list is a seed of 27 phrases, and seven rows were declined.** Source: the Turkish block of `sachaarbonel/whisper-hallucinations` (MIT, `phrases.csv`), 24 rows, extracted during the market study; 17 of them are here. The other seven — `abone ol`, `afiyet olsun`, `görüşürüz`, `olsun`, `ayı unutmayın`, `mayın teşekkürler`, `tıklamayı unutmayın` — are things a person can plausibly dictate as the whole of an utterance, or mid-word truncations of sentences the list already carries in full, so they stay in the source with the reason beside each rather than being silently dropped. Ten more come from field data: the phrase our own robustness run actually produced, and the full sentences the dataset only holds truncated. The closest call in the list is `beğenmeyi unutmayın` itself, kept because the whole-segment rule is what protects the genuine one and because the list is configuration, not a constant.
  - **The default dictionary is empty, and that is the decision.** Every entry is a claim about one person's vocabulary; shipping a stranger's terms would put words into transcripts that were never spoken, which is the one failure a dictation tool may not have. `apply` replaces a variant with the canonical spelling, whole words only, and keeps the Turkish suffix that was attached to it — `keş'i` becomes `cache'i`, `cache'i` is left alone — because the match is against the part in front of the apostrophe. Matching is case-insensitive under **both** Turkish and Unicode casing, since the two lowercasings disagree on exactly one letter and both disagreements are real (`CLI` would never find `cli` under Turkish rules; `IŞIK` would never find `ışık` under Unicode ones). It is deliberately **not** diacritic-folded: folding would let the variant `keş` swallow the ordinary word *kes*. `is_term` exists so the casing rule asks before it capitalises, which is what keeps `cron çalışıyor.` from becoming `Cron`.
  - **The prompt is capped at 180 characters of terms, pinned first and then most recently added.** Whisper's prompt window is 224 tokens and Turkish tokenises into sub-word pieces, so a character budget is the conservative reading of a token limit; the cap is a named constant rather than a number repeated in two places. A term that would cross the budget is left out whole, so the same dictionary always produces the same prompt — which matters because M0 measured the prompt as *the* accuracy feature (0.379 → 0.261, 17 → 30/31 terms) and a prompt that varied between runs would make that number unreproducible. The carrier sentence the M0 runs wrapped the list in belongs to `dile-engine`, not here.
  - **What the tests are.** 156 before→after corpus rows in `crates/dile-core/tests/corpus.rs` — 47 light, 48 medium, 47 strict, 14 dictionary — plus 22 unit tests inside the crate and two doctests. The rows where input and expected are equal are not padding: at every level, what the rules refuse to touch is as much of the specification as what they change. Three properties a table cannot state are tested separately: cleaning twice is the same as cleaning once (the panel re-runs cleanup every time the strictness chip is touched), no level ever strips a diacritic, and the report's steps chain into one another. **The fixtures are written by hand with neutral vocabulary** — the real ASR output that taught us the error shapes lives in the benchmark workspace with the maintainer's own recordings, and copying those sentences in would publish them.
  - **Two known false positives, written down rather than patched over.** `Ne yazık ki geç kaldık.` starts with *ne* and is not a question, so §5's question-word rule puts a `?` on it; the alternative is a list of exclamative openers, which is a second guess layered on a first, and the panel shows the result before it is pasted. And the question particle is only recognised as the **last** word of a sentence, so `Bunu şimdi mi yapayım.` keeps its full stop — deliberate, because a particle in the middle of a sentence is not reliably the sentence's own question. `müdür` is not in the particle list at all: it is also the noun *director*, and `Ahmet müdür.` is a statement.
  - **The filler rule has nothing to do, and one of its tests says so.** §8's 09-09 entry measured 20 sentences across 8 engine rows with zero vocalised fillers, so `\b[Ee]{2,}\b` ships as a guard for engines that do write them rather than as a feature. `extra_fillers` is the opt-in the 09-07 decision asked for and it is empty.
  - Performance: `clean` over 2,006 characters at the strict level runs in **245 µs** in release on the maintainer's machine, against a target of under a millisecond. The test asserts a far looser bound, because a timing test that fails on a loaded machine teaches nothing.
  - Still deliberately absent: anything that needs audio or a window. The dictionary has no persistence — a settings file is WP5's — and the duration gate that the 09-09 finding pairs with the phrase filter lives in `dile-capture`'s VAD summary, which is already dropping recordings with no speech in them.
- 2026-09-13 — **WP2 is implemented: the application records.** Three commits, `e94d244`…`c920cb4`, on top of the two crates written the same day (`1e65b34`…`e275145`). Holding `Ctrl+Alt+Space` now opens the microphone, keeps the half second in front of the press, and hands back a 16 kHz mono buffer with a verdict on whether anything was said. What is *not* claimed: the acceptance criteria of WP2 are about a person holding a key, and the local gate ran on a machine with no microphone in the loop, so **hardware acceptance waits for the maintainer's hand test** (`docs/BUILDING.md`, "Trying the capture path").
  - **`dile-hotkey` — the trigger as a clock-free state machine.** Every event carries the instant it happened at and the machine reads no clock of its own, so the twelve press/tap/toggle/withdraw scenarios are ordinary unit tests instead of a hand test with a stopwatch; the Windows adapter over `handy-keys` is then thin enough to hold no rules at all. **Recording starts at the press**, before anyone knows whether the press will turn out to be real, which is what makes `DiscardRecording` a normal outcome rather than an error path: a press under the 250 ms threshold throws its own recording away and opens the panel idle.
  - **The blocking decision, which WP0 deliberately left open.** The hook is installed **blocking** with exactly one hotkey in the set — the primary chord — because an observed-only `Ctrl+Alt+Space` still reaches the focused window and puts a space in the editor the user is dictating into. `handy-keys` carries the piece a swallowed Alt chord needs on Windows: a menu mask, so that releasing Alt after a blocked key is not read as a lone tap that arms the window's menu bar. **The second key is never in that set.** It is a bare right Ctrl; blocking it would take `Ctrl+C` away from every application on the machine, and a lone modifier has nothing to swallow anyway — it means nothing until another key joins it, which is the case the state machine already resolves by starting and withdrawing. §3's Hotkey row carries the decision.
  - **`dile-capture` — the microphone, and the two things beside the audio.** `cpal` callback → lock-free SPSC ring → worker thread (downmix, `rubato` to 16 kHz, pre-roll ring, cap, meter, VAD). Nothing but a push happens in the audio callback, and samples that do not fit when the worker is descheduled are counted as overruns rather than waited for. The **pre-roll ring is the first-word protection** the acceptance criterion asks for, and the recording is **never trimmed**: the VAD produces a summary next to the audio and cannot shorten it, because a detector that cuts is a detector that can cut a word.
  - **The VAD floor finding, from real hardware.** The obvious design — learn the noise floor only while nobody thinks it is speech — latches, and it latched the first time the crate met a room: a machine whose floor sits at −49 dBFS is above the absolute minimum threshold, so the first transient starts a hangover, the hangover suppresses every floor update, the threshold stays pinned, and the next transient re-arms it. **Four seconds of an empty room came back as 3.84 s of speech.** What makes learning *during* speech safe is the ceiling on the floor rather than the decision: speech cannot teach the detector that speech is the background, because the floor is not allowed above a level no room reaches (−40 dBFS). The asymmetry does the rest — down fast, up slowly. This is the baseline the M0 protocol asked for as a floor, not the winner: **`earshot` vs Silero is still unmeasured**, and the trait seam is there so the winner is a constructor argument.
  - **The wiring, in `dile-app`.** One thread consumes the hotkey's four verbs and owns the capture; a second forwards the level stream. The tray now carries the state in its icon and its tooltip — idle, recording, working, nothing heard, no microphone — which closes the placeholder WP0 left ("the state icon is WP2's"). Level readings and state changes leave as the Tauri events `dile://level` and `dile://state`, aimed at the panel window WP5 will fill. **A recording whose VAD summary finds no speech is dropped**, and the tray says so: the silence half of the hallucination guard that the 09-09 robustness run handed to this package, since every canned hallucination in that set sat on the same ten seconds of applause. A microphone that will not open is reported on the tray and retried at the next press; a keyboard hook that will not install is fatal, because every feature of this product begins with a key press.
  - **`last.wav`, and why it is not a privacy hole.** A debug build writes the last recording that contained speech to the application's local data directory, because "no clipped first syllable" is a criterion a person has to *listen to*. The whole function is behind `#[cfg(debug_assertions)]`, so a release build does not merely skip it — it does not contain it. The README's "audio is processed in memory, never written to disk" therefore stays true of every build anyone can install, and is false only of a build that has to be compiled from source with a debug profile.
  - Still deliberately absent: the panel (WP5), the engine (WP3 — `dile-engine` is still not a dependency of the tray), a settings file, and a display form for the chord. The tooltip still names `Ctrl+Alt+Space` as a constant, because turning a `Chord` into a string is the settings window's job and the constant is true until the chord can be changed.
- 2026-09-13 — **Two decisions taken, and the documents made to match them.** Maintainer decisions: (A) **the v1 model is stock large-v3 `q5_0` with the dictionary prompt** — no fine-tune ships in v1; (B) **Vulkan is the default tier, decided by a probe transcription in the isolated engine process on first start, with CPU as the fallback tier and a small model when the probe fails.** Both replace the 09-07 "CPU default, GPU opt-in" row, and both follow the M0 evidence rather than the original plan: a CPU-default product cannot feed the dictionary to any model fast enough to use, which is the one thing that moved accuracy. §2, §3, §6 WP3 and §7 are rewritten around them, with the rejected alternatives and their numbers kept next to the decision so the trade is still visible.
  What else changed, and why it was overdue: **the README's "raw Whisper output in Turkish is full of fillers, stutters" claim is gone** — this file had been carrying a note to rewrite it since the 09-09 M0 run, because our own data shows no supported engine writes a vocalised filler, and a public README contradicting its own repository's measurements is worse than no README. The truthful pitch is the dictionary → prompt path (0.379 → 0.261, 17 → 30/31 terms) and Turkish casing and punctuation. §4 gains a table saying **which M0 numbers are final and which are still waiting on the Dilim B reference**, so no figure here gets quoted with more authority than it has; the term-heavy slice is still unadjudicated and its cells stay empty. References to material outside this repository — local paths, private notes and recordings — are reduced to what a reader outside the project can actually verify or safely ignore. The Actions failure is described as a mechanism rather than an account state: **CI on the private repository is gated locally by `scripts/ci-local.ps1` until the repository is public.**
  Dependency hygiene, same day: `cargo deny check` went from eleven errors to green without a blanket ignore. `time` 0.3.45 → 0.3.47 (RUSTSEC-2026-0009) and `plist` 1.8.0 → 1.10.1, which carries `quick-xml` 0.38.4 → 0.42.0 (RUSTSEC-2026-0194/0195); the path dependency on `dile-core` now carries a version, because a bare path is a wildcard; `Apache-2.0 WITH LLVM-exception` is allowed as the permissive licence it is. Both security fixes declare Rust 1.88, so **`workspace.package.rust-version` moves 1.85 → 1.88** — taking a fix and then claiming an MSRV the fixed tree cannot build under would be a lie in the manifest. What is left in `[advisories] ignore` is six unmaintained-crate advisories with no fixed version to move to (`proc-macro-error` and the five `unic-*` crates `tauri-utils` pulls through `urlpattern`), each with its reason written beside it.
- 2026-09-09 — **Robustness layer scored; the agreement rule holds where it matters.** Three creator-subtitled videos (44:52), no prompt, normalized WER (benchmark workspace, `results/2026-09-09-m0-robustness`): `tc-q5_0` **0.0608** · `tc-large-v3` 0.0624 · `tc-turbo` 0.0685 · `fw-large-v3-beam5` 0.0685 (exact tie with turbo) · Qwen3-ASR-1.7B 0.108. **Among the three `transcribe-cpp` rows — the only ones that can select the v1 model — the order is identical to the 20-sentence set: q5_0 < large-v3 < turbo (ρ = 1.0).** The one row that moves is the CTranslate2 information row: best on the maintainer's voice, tied-last on independent speakers; it has no Rust path, so nothing changes, but it is a reminder that beam search alone does not travel across speakers. Absolute numbers are not comparable across layers (edited subtitles vs verbatim references); only the order transfers. Findings for later packages: (a) **all canned hallucinations sit in the same 10 s of applause** at one TEDx opening (`izlediğiniz için teşekkürler`; two rows reproduced the translator-credit card) — an insertion over non-speech, so WP2's VAD / silence gate, not the decoder, owns it; `tc-large-v3` was the only clean whisper row; (b) `beğenmeyi unutmayın` was genuinely spoken in the screencast — WP4's hallucination filter must cross-check against duration/segment shape, never delete on phrase match alone; (c) **on long audio turbo is 7× faster than q5_0 and 17× faster than large-v3** (RTF 0.039 / 0.288 / 0.682) for +0.008 WER — the 300 s recording cap makes this relevant; (d) **`qwen3_asr` does not chunk and stops at a 256-token generation cap** (`OutputTruncated`, status 18) on a 9-minute file — with a 60–300 s cap Dile would need its own chunking for any Qwen row, another mark against a Qwen CPU tier; (e) zero decoder loops on all 15 long transcripts.
- 2026-09-09 — **M0: the prompt is the accuracy feature; the CPU tier is a model-family question; nobody writes "eee".** Results in the benchmark workspace, `results/2026-09-09-m0-prompt-and-small`. Three measurements on the same 20 sentences, and what they decide:
  1. **Dictionary → initial prompt (`WhisperRunOptions::initial_prompt`, the only biasing knob in `transcribe-cpp` 0.2.3; no hotwords field exists).** One Turkish sentence carrying the term list, identical on every row: `tc-q5_0` WER **0.379 → 0.261**, terms **17 → 30/31**; `tc-large-v3` 0.385 → 0.273, 30/31; `tc-turbo` 0.422 → 0.348, 28/31; `fw beam5` 0.354 → 0.255. **Zero damage**: no prompt word appeared where it was not spoken, no term lost on any row; 8 of the 9 all-miss terms recovered (only `token` stays `takım`, a real Turkish word that fits the slot). The prompt recovers about three times what beam search did, and with it the terms-category WER converges to 0.206 on four of five rows — **with the dictionary in place, the model choice stops mattering on Dile's material.** Cost on `transcribe-cpp`: p95 +0.4 s (`tc-q5_0` 1.96 → 2.38 s, `tc-turbo` 0.67 → 1.04 s); free on CTranslate2. **Binding:** WP4's dictionary feeds the prompt from day one; the M0 latency budget is measured *with* the prompt; every README accuracy claim is stated with the dictionary on.
  2. **Small models for the CPU tier** (GGUFs from `handy-computer`, pinned + sha256 = HF LFS oid; architecture auto-detected from `general.architecture`): **Qwen3-ASR-0.6B F16 is the first CPU row faster than real time** (RTF 0.27, p95 1.37 s) but WER **0.491**, terms 11/31, and **none of the small models accepts a prompt** (`accepts_ext(Run,'WHRN') = false`). Qwen3-ASR-1.7B Q8_0: WER 0.422, CPU p95 1.84 s. **Nemotron 3.5 ASR 0.6B is rejected**: it refuses `language=tr` (`status 10, unsupported language`) and without a hint emits `<sl-SI>`, Arabic script and an empty string. Oddity: Vulkan is *slower* than CPU for both Qwen sizes on this machine. **Consequence, proposed for the maintainer's decision (changes the §3 GPU row):** a CPU-default product cannot use the dictionary and dictates at WER ≈ 0.49; the honest shape is **Vulkan default when a device passes a first-run probe transcription in the isolated engine process, CPU as the fallback tier** (whisper q5_0 with prompt at ~10 s latency, or Qwen-0.6B at 1.4 s without the dictionary — the user picks "accurate" or "fast"). Handy #1755's BSOD is a driver-level risk the process boundary cannot contain, which is why the probe and an always-visible toggle stay.
  3. **No engine writes a vocalised filler.** Over 20 sentences × 8 rows, zero hits for `eee|ııı|aaa|hmm|mmm|eh|ah`; every whisper row also collapsed the deliberate stutter in #15 (`bir daha, bir daha` → `bir daha`), both Qwen sizes preserved it. **So the light-level `\b[Ee]{2,}\b` rule has nothing to do on any supported engine**; it stays as a guard, not a feature. Cleanup's real v1 work is punctuation normalisation (the prompt's comma habit already lifted punct F1 from 0.63–0.72 to 0.75–0.82), Turkish casing, dictionary spelling, the hallucination filter, and — for Qwen only — repeat handling. **The README's "raw Whisper output in Turkish is full of fillers, stutters" sentence is contradicted by our own data and is rewritten before release**; the truthful headline is the diacritic-safe dictionary that feeds the recogniser, measured on the maintainer's own voice.
  Draft v1 pick on the GPU tier, pending the Dilim B reference: **q5_0 large-v3 with the dictionary prompt** (WER 0.261, 30/31 terms, p95 2.38 s) over turbo (0.348, 28/31, 1.04 s; Icelandic hallucination on #02 survives the prompt). The 2.0 s budget written earlier tonight is exceeded by 0.4 s by the accurate pick; whether Dile spends that 0.4 s on accuracy is the maintainer's call, and the panel's 1.5 s countdown starts only after the text is there, so the felt wait is the transcription time alone.
- 2026-09-09 — **First M0 table, on the 20-sentence set** (77 s of the maintainer's real dictation-length utterances; decoder parity: `tr`, T=0, no fallback, no prompt, no VAD; results in the benchmark workspace, `results/2026-09-09-m0-sentences`). Rows: `transcribe-cpp` large-v3 F16 / turbo F16 / q5_0 on Vulkan and CPU (decision layer, greedy), faster-whisper large-v3 CUDA beam 5 and greedy (information layer).

  | row | device | p50 / p95 s | WER | CER | terms /31 | notes |
  |---|---|---|---|---|---|---|
  | tc-large-v3 F16 | Vulkan | 3.40 / 4.17 | 0.385 | 0.180 | 16 | slowest GPU row |
  | tc-turbo F16 | Vulkan | **0.63 / 0.67** | 0.422 | 0.191 | 16 | worst on term sentences (0.735); Icelandic hallucination tail on #02 |
  | tc-turbo | CPU | 8.67 / 10.18 | 0.410 | 0.188 | 16 | RTF 2.3 |
  | tc-q5_0 | CPU | 8.57 / 14.12 | 0.385 | 0.186 | 17 | RTF 2.6 |
  | **tc-q5_0** | **Vulkan** | **1.47 / 1.96** | **0.379** | 0.182 | **17** | best decision-layer WER, 1/3 the size of F16 |
  | fw-large-v3 beam 5 | CUDA | 0.53 / 0.65 | 0.354 | 0.166 | 18 | information only |
  | fw-large-v3 greedy | CUDA | 0.47 / 0.57 | 0.391 | 0.187 | 16 | sits on top of tc-large-v3 → the gap is the decoder |

  What the table says, as judgement rather than numbers:
  - **The model choice is not the main lever.** All seven rows land at WER 0.35–0.42 and recall about half of the technical terms; nine terms are missed by every row, among them `cron→kron`, `CUDA→judo`, `Tauri→Tower`, `rdev→RDV`, `token→takım`, `cache→Cage` and `license→lisans`; the remaining two are product names that came back as Turkish words. The spread between models (≈ 4 points) is the size of the beam-vs-greedy effect (3.7 points). **The dictionary → initial-prompt path (WP4) is therefore the product's accuracy feature**, and M0 measures its gain next, with the same rows and a prompt carrying the term list.
  - **"CPU default" is not viable with any whisper model on this 6-core CPU:** even turbo is 2.3× slower than real time, plus ~10 s cold start. Either a genuinely small model (Qwen3-ASR-0.6B, Nemotron 0.6B — M0 measures them next) makes the CPU tier real, or the default becomes Vulkan with the isolated engine process as the crash guard, and CPU stays as the fallback tier. Decision after those rows.
  - **q5_0 beats F16 large-v3 on Vulkan** (0.379 vs 0.385) at 2.3× the speed and a third of the size; the quantization costs nothing here. On the GPU tier q5_0 is the draft pick; the literal "latency > 2× best" rule would hand the choice to turbo (p95 0.67 s), which is the least accurate row on the term sentences and the one that hallucinated — **the rule is rewritten**: on the GPU tier, eligible = p95 ≤ 2.0 s for these 3–6 s clips, then lowest WER on the term-heavy material. Draft only until the Dilim B reference is adjudicated.
  - **Whisper already removes "eee" and repairs the stutter** in all seven rows (`Eee, bu kısmı bir daha, bir daha söyleyeyim.` → `Bu kısmı bir daha söyleyeyim.`). The light-level filler regex therefore has nothing to do on whisper output; it stays for engines that do write fillers and as a guard, not as a headline feature. README wording to be adjusted before release.
  - Numbers: every row wrote digits (`10 50 42 56 2026 14 25 75`), 11/11 correct. Questions: `?` present on 4/4 for most rows. No canned YouTube hallucination anywhere.
  - **Vulkan first-run shader compile ≈ 7 s** on a cold process (1.7 s warm): WP3 pre-warms the engine at startup and after model change.
  - Caveat: CPU rows were run with the Vulkan-feature binary and `--backend cpu`; nvidia-smi under-reports Vulkan VRAM on WDDM.
- 2026-09-09 — **GPU probe, Dilim B hypotheses, adjudication tool.** Vulkan works on the RTX 5060 (`whisper on Vulkan0`, no crash): large-v3 GGUF RTF **0.73**, legacy `.bin` 0.83, turbo GGUF **0.032**; CTranslate2 CUDA fp16 large-v3 0.39 (needs the cuBLAS/cuDNN wheel `bin` dirs on the DLL path — `os.add_dll_directory`, otherwise `cublas64_12.dll is not found`). Two facts about **`transcribe-cpp` 0.2.3 that reshape WP1**, both verified in its source and by running it:
  - **It has no beam search.** Zero occurrences of "beam" in the crate, the `-sys` crate and `transcribe.h`; the whisper path samples greedily (argmax at T=0, multinomial at T>0). So "fix beam size identically for every candidate" (the entry below) is **impossible inside the decision runtime**; within it every model is compared greedy vs greedy, and faster-whisper rows with `beam_size=5` are information only. On Dilim B the greedy large-v3 was the only engine that mangled the four Latin product names in the take — it diacritised or re-spelled all four — while beam-5 large-v3 and greedy turbo wrote them correctly, so the proper-noun difference seen in Step 0 was the decoder, not the weights. Consequences: (a) the dictionary → initial-prompt / hotword path (WP4) is not a nicety, it is the compensation for greedy decoding and must be measured in M0 with and without; (b) an upstream feature request for beam search is a legitimate later contribution; (c) turbo may win the term-recall column despite its independent Turkish WER, which our set has to settle.
  - **No word timestamps** (`unsupported timestamp granularity (status 12)`); segment granularity only. Irrelevant for dictation, relevant for tooling: the adjudicator interpolates word times inside segments and labels them.
  - `RsGoksel/RsGoksel_ITU_Mainframe` is a **transformers/safetensors** repo, not CTranslate2 as §3 says; it needs conversion before even an information-layer row. §3 wording corrected by this entry.
  - Dilim B disagreement across `fw-large-v3` (beam 5), `tc-large-v3`, `tc-turbo`: **8.0 % of 675 word columns**, 36 regions, 71.8 s of disputed audio → adjudication ≈ 5 min of maintainer time. All three engines re-spelled the same class of word: English product names and technical terms turned into the nearest Turkish-sounding spelling (`Cage` for cache, `Kodeks` for a tool name, `kron` for cron) — the dictionary's job list, in the maintainer's own voice.
  - The 20-sentence set is recorded (20 files, 2.7–6.0 s, no clipping; sentence 11 deliberately swaps push/pull and its reference says so).
- 2026-09-09 — **M0 Step 0 done: both layers run on the maintainer's machine.** Installed: CMake 4.4.3, Vulkan SDK 1.4.357, Python venv (faster-whisper 1.2.1, ctranslate2 4.8.2, jiwer 4.0.0, cuBLAS/cuDNN wheels). Models pinned and sha256-verified against HF LFS: `ggerganov/whisper.cpp` @ `5359861c` (`ggml-large-v3.bin`, `-turbo`, `-q5_0`) and `handy-computer/*-gguf` F16 pair. Findings, each binding on later packages:
  - **`transcribe-cpp` 0.2.3 is on crates.io, MIT** (no git dependency). `default-features = false` is mandatory on Windows because the crate's default feature is `metal`; the Vulkan feature is literally `vulkan`; Handy adds `dynamic-backends`. → WP0 `Cargo.toml`.
  - **Vulkan build needs one env var the SDK installer does not set:** the link fails with `LNK1181: 'vulkan-1.lib'` until `LIB=%VULKAN_SDK%\Lib;%LIB%`. Everything else (1,977 SPIR-V shaders via `glslc`, `ggml-vulkan.lib`) builds unattended; ≈ 8 min from cold, CPU-only build ≈ 3 min. → WP0 CI recipe and `docs/BUILDING.md`.
  - **GGUF vs ggml:** the crate's API documents GGUF only (`TRANSCRIBE_ERR_GGUF`, models from `handy-computer/*-gguf`), yet the legacy `ggml-large-v3.bin` loaded and transcribed correctly. Decision: **the pinned v1 source is the documented format (GGUF, `handy-computer` repos)** unless WP1 measures a difference; loading `.bin` is undocumented behaviour and is not relied on. §3 Distribution row's `ggerganov` wording is superseded by this.
  - **Decoder parameters move proper nouns.** Same large-v3 weights, same 81 s take: faster-whisper with `beam_size=5` wrote the take's four Latin product names correctly, while transcribe-cpp with its defaults diacritised or re-spelled every one of them. → WP1 fixes beam size, temperature and prompt **identically for every candidate row** before any WER is compared, and the term-recall column is measured with and without the dictionary prompt.
  - **large-v3 on this CPU is real-time-factor ≈ 1.0** (81 s of audio → 79 s in transcribe-cpp, 84 s in CTranslate2 int8, quiet 6-core machine). For 5–10 s dictations that is 5–10 s of waiting: **"CPU default" cannot mean "large-v3 on CPU"**. WP1's latency column decides the CPU-default model (q5_0, turbo, Qwen3-ASR-0.6B or Nemotron are the candidates); large-v3 may remain the GPU-opt-in choice.
  - `result.language` returns nothing from transcribe-cpp even with a language hint — check in WP3 if the field is wanted.
  - GPU probe deferred: 7.0 of 8.1 GB VRAM was in use by another process throughout; the Vulkan binary is built and the command is written in the benchmark workspace, `rs-probe/GPU-PROBE.md`.
- 2026-09-09 — **Robustness set assembled and decision-layer audio prepared** (both outside the repo, in the benchmark workspace). Environment inventory: Rust 1.98 msvc, ffmpeg 9, Vulkan runtime (loader only), Python 3.13 without ASR packages; the only model on disk is the CTranslate2 large-v3 snapshot — **no ggml/gguf weights yet**, so Step 0 installs Vulkan SDK + CMake and downloads ggml large-v3 before any `transcribe-cpp` run.
- 2026-09-09 — **M0 test material split into two layers.** Maintainer's idea: score engines against YouTube videos that carry subtitles. Adopted with guards: only creator-uploaded subtitles (auto captions are circular), normalized WER for ranking only, audio stays local and unpublished, and this layer cannot select the v1 model. In exchange the maintainer's own layer shrinks to the term-heavy slice + the 20 sentences (≈ 20 min of maintainer time instead of 30–40). Agreement between the two rankings is recorded in this file either way.
- 2026-09-09 — **Interaction model decided: review panel, not blind paste.** Maintainer's three decisions: (1) default hotkey **`Ctrl+Alt+Space`**, hold by default, toggle as a setting, `Right Ctrl` modifier-only kept as the optional second key; (2) after release the cleaned text is shown in a Spotlight-sized grey, non-activating panel with a live level meter during recording and is **auto-transferred after 1.5 s unless ✗ or editing**, switchable off; (3) the text is **editable in the panel** before ✓. Copy button copies without pasting and without clipboard restore. §3 gains a "Panel" row, WP2 gains the level stream, WP5 becomes "Panel + paste + settings", §7 gains the review row. Engine, cleanup, distribution decisions untouched.
- 2026-09-09 — Maintainer identity: author, publisher and copyright are Furkan Yıldız; bundle identifier moved to `io.github.xfurqan0.*` before any release. The public repository starts from a single root commit and the full development history is kept privately, so the published tree carries one identity from its first commit rather than a rename recorded in a blame view — and when WP0 writes `tauri.conf.json` it takes `io.github.xfurqan0.dile` as the identifier and `Furkan Yıldız` as the publisher, rather than being renamed later. Settled here for the same reason as in the two sibling repositories: a bundle identifier is a product identity to Windows, and changing one after a release is a new product, not a new version.
- 2026-09-09 05:25 — **WP0 done: the repository is a workspace, and CI gates it.** Eight commits, `39d7068`…`b2dd8bf`. Three crates, copied in structure and convention from `nazar-tray` and diverging only where Dile's needs differ:
  - **`dile-core`** — the Turkish layer with no Tauri, no audio and no engine in it, so it builds anywhere. `cleanup` carries the three strictness levels as a type, `dictionary` carries entries that keep `ç ğ ı ö ş ü` byte for byte, `normalizer` implements the one rule that could not wait: Turkish casing, `i → İ` and `I → ı`. Everything else returns its input untouched **on purpose** — the rules come from the edit-log corpus, and the first draft of this file is the evidence for what happens when a rule is written before its data.
  - **`dile-engine`** — `transcribe-cpp = "0.2.3"`, `default-features = false`, Vulkan behind a `gpu-vulkan` cargo feature. Asking for the GPU from a CPU build is a **typed error**, not a silent fall back: a user who enabled the GPU and got the CPU would file a performance bug rather than a build one. The decoder settings of the M0 parity baseline — language hint, temperature 0, no prompt, segment timestamps — live in one function, because there is no beam size in this runtime to fix alongside them.
  - **`dile-app`** — tray icon, a menu with Settings and Quit, **no window shown at start**. `io.github.xfurqan0.dile`, publisher Furkan Yıldız, NSIS + MSI configured and neither built until WP7. `dile-engine` is deliberately **not** a dependency: WP3 puts the engine in its own process, and linking it now is the architecture WP3 would have to undo.

  Verified locally before pushing: `cargo fmt --check`, `cargo clippy --workspace --all-targets -D warnings`, `cargo test --workspace` (18 tests). The `gpu-vulkan` feature **builds** (3 m 44 s with `LIB=%VULKAN_SDK%\Lib;%LIB%`, SDK 1.4.357), which closes the last open question of §7: the Vulkan SDK **is** needed on a machine that builds the GPU path, and nothing else is. The engine's ignored smoke test ran on `take-02.wav` with `whisper-large-v3-turbo-F16.gguf`: **CPU, RTF 0.381** (81.2 s of audio in 30.9 s, 1.15 s load, 24 segments) — the first CPU number for turbo, and a useful contrast with the 1.0 that large-v3 posts on this machine.

  Three decisions taken while building, each a small divergence from nazar-tray:
  - **No npm, no frontend build step.** `frontendDist` points at `ui/` as static markup. nazar-tray bundles its panel with esbuild because it has a panel; this repository has empty markup, and an `npm ci` in CI to compile nothing is runner minutes and a lockfile to keep current for no output. WP5 turns `ui/` into `ui/src` + `ui/build.mjs` and moves `frontendDist` to `ui/dist` — the same shape, arrived at when there is something to compile.
  - **The locale files live at the repository root**, not under `ui/`, because the Rust side is currently their only reader. The format, the flat-string rule and the parity test are nazar-tray's unchanged.
  - **The no-hard-coded-text test is written in Rust** rather than in Node, for the same reason. It reads `crates/dile-app/src` and `ui/index.html`, throws away comments, unit-test modules and diagnostic macros, and fails on a literal that reads like a sentence.

  **CI has not run once, and the reason is not the code.** Actions was unavailable on this private repository, so both jobs were rejected before a runner started; a re-run behaved identically, and nothing in the repository can change it. **So `.github/workflows/ci.yml` is unverified by a real run**, and WP0's acceptance criterion is not met yet even though every step in that file passes on the maintainer's machine. CI on the private repository is gated locally by `scripts/ci-local.ps1` until the repository is public: it runs the same steps in the same order against a fresh clone of `HEAD`, which is the closest thing to a runner that exists here.

  Not decided and left for WP2: the tray icon is the application icon and a placeholder — a waveform mark drawn to have something the right shape in the tray; the state icon (idle / recording / working) is WP2's, and so is the hotkey the tooltip currently names as a constant.
